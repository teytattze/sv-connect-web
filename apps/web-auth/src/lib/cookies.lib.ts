// import { Nullable } from '@sv-connect/shared';
// import cookies from 'js-cookie';

// export const getCookie = <TData = unknown>(name: string): Nullable<TData> => {
//   const data = cookies.get(name);
//   return data ? JSON.parse(data) : null;
// };
